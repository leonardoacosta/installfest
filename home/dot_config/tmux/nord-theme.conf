# =============================================================================
# Nord Theme for tmux
# https://www.nordtheme.com/
# =============================================================================

# Nord Palette
# Polar Night:  #2E3440 #3B4252 #434C5E #4C566A
# Snow Storm:   #D8DEE9 #E5E9F0 #ECEFF4
# Frost:        #8FBCBB #88C0D0 #81A1C1 #5E81AC
# Aurora:       #BF616A #D08770 #EBCB8B #A3BE8C #B48EAD

# -----------------------------------------------------------------------------
# Status Bar General
# -----------------------------------------------------------------------------

set -g status-style "bg=#2E3440,fg=#D8DEE9"

# Status bar length
set -g status-left-length 50
set -g status-right-length 100

# -----------------------------------------------------------------------------
# Status Left - Session Name
# -----------------------------------------------------------------------------

set -g status-left "#[bg=#81A1C1,fg=#2E3440,bold] #S #[bg=#2E3440,fg=#81A1C1]"

# -----------------------------------------------------------------------------
# Window Status - Tab-like Appearance
# -----------------------------------------------------------------------------

# Separator between windows
setw -g window-status-separator ""

# Inactive windows (tabs). #(cc-tmux window-icon) is the animated tab icon
# (apps/cc-tmux render.animated_icon) — re-invoked on every status-bar
# refresh (status-interval), which drives the animation; it prints its own
# trailing space when tracked, nothing otherwise (no literal space before #W).
setw -g window-status-format "#[bg=#3B4252,fg=#2E3440]#[bg=#3B4252,fg=#4C566A] #I #(~/.tmux/plugins/cc-tmux/bin/cc-tmux window-icon #{window_id})#W #[bg=#2E3440,fg=#3B4252]"

# Active window (current tab) - using frost teal
setw -g window-status-current-format "#[bg=#88C0D0,fg=#2E3440]#[bg=#88C0D0,fg=#2E3440,bold] #I #(~/.tmux/plugins/cc-tmux/bin/cc-tmux window-icon #{window_id})#W #[bg=#2E3440,fg=#88C0D0]"

# Window with activity
setw -g window-status-activity-style "none"

# -----------------------------------------------------------------------------
# Status Right - Clock and Host
# -----------------------------------------------------------------------------

set -g status-right "#[bg=#2E3440,fg=#3B4252]#[bg=#3B4252,fg=#4C566A] %H:%M #[bg=#3B4252,fg=#81A1C1]#[bg=#81A1C1,fg=#2E3440,bold] #h "

# -----------------------------------------------------------------------------
# Status Format Rows 2 & 3 - cc-tmux session/usage + beads bars
# -----------------------------------------------------------------------------

# Row 2 (session-bar): session-count glyph + model-letter + project>branch on the
# left, account + SES/5H/7D usage gauges on the right. cc-tmux emits its own
# semantic fg color codes internally (same as status-right's usage segment); the
# bg wrapper matches this theme's status-style background so the row blends in.
set -g status-format[1] "#[bg=#2E3440]#(~/.tmux/plugins/cc-tmux/bin/cc-tmux session-bar #{window_id})"

# Row 3 (beads-bar): roadmap-pulse content (next actionable item + open/ready
# counts), or empty when nothing is pending.
set -g status-format[2] "#[bg=#2E3440]#(~/.tmux/plugins/cc-tmux/bin/cc-tmux beads-bar #{window_id})"

# -----------------------------------------------------------------------------
# Pane Borders
# -----------------------------------------------------------------------------

set -g pane-border-style "fg=#434C5E"
set -g pane-active-border-style "fg=#88C0D0"

# -----------------------------------------------------------------------------
# Message/Command Line
# -----------------------------------------------------------------------------

set -g message-style "bg=#81A1C1,fg=#2E3440,bold"
set -g message-command-style "bg=#81A1C1,fg=#2E3440"

# -----------------------------------------------------------------------------
# Mode (copy mode, etc.)
# -----------------------------------------------------------------------------

setw -g mode-style "bg=#81A1C1,fg=#2E3440"

# -----------------------------------------------------------------------------
# Clock
# -----------------------------------------------------------------------------

setw -g clock-mode-colour "#88C0D0"
