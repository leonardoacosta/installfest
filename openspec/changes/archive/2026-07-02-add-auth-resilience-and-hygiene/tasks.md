<!-- beads:epic:if-vit -->
<!-- beads:feature:if-49r -->
# Implementation Tasks

## Auth Batch

- [x] [1.1] [P-1] Create `scripts/az-reauth-nudge.sh` — reads MSAL cache metadata (timestamps only) for `~/.azure-bbadmin` + `~/.azure-o365`, computes per-identity token age (independent clocks), notifies via `nx_notify` once per identity per window when within lead margin of the 60d CA wall. Window 60d + margin 5d, env-configurable (design.md D1). Dedup state in `~/.local/state/az-reauth-nudge/`. Exit 0 always. IMPLEMENTATION NOTE: MSAL cache metadata (cached_at/last_modification_time) tracks silent refresh, not interactive login, and never advances the 60d clock — verified live against both identities' real caches. Primary age source is a login-epoch stamp written by `executable_az` on `az login` success (design.md D3); cache mtime is the fallback. Verified live: fires once at window=0, second run dedups silently (task 4.2). [owner:general-purpose] [beads:if-oc0]
- [x] [1.2] [P-1] Create `home/dot_config/systemd/user/az-reauth-nudge.{service,timer}` (timer: OnCalendar=daily, Persistent=true — design.md D1) and register both in `run_onchange_after_install-user-schedulers.sh.tmpl` (Linux block; embed sha256sums per house pattern) [owner:general-purpose] [beads:if-r4t]
- [x] [1.3] [P-1] Create `home/dot_local/bin/executable_mx-token` — broker socket token client per Req-2, mirroring `git-credential-mxbroker.sh` hardening (ownership check, --max-time 5, silent-fail exit 0, no token logging). Verified live: returns a real JWT against the live broker socket, empty+exit 0 with the socket absent (task 4.3). [owner:general-purpose] [beads:if-yhr]
- [x] [1.4] [P-2] Extend `home/run_after_doctor.sh.tmpl` — warn when MSAL cache age is already past the CA window (login will fail on next az call) alongside the existing broker-socket check. Linux branch (was `true` placeholder) now carries this — the darwin branch's mx-broker check stays as-is. Verified: rendered + ran clean (silent, fresh cache) on homelab. [owner:general-purpose] [beads:if-ean]
- [x] [1.5] [P-1] Fail-fast on AADSTS70043 in `home/dot_local/bin/executable_az` — detect 70043 in stderr of a failing call, notify once with the re-login command, set per-identity marker in `~/.local/state/az-reauth-nudge/`; while marker exists, short-circuit that identity's calls with a one-line error; clear marker on successful `login` invocation (design.md D2). Shebang changed sh->bash (needed for `2> >(tee ...)` process substitution that streams stderr live while capturing it for the 70043 check — device-code output must stay interactive). Verified end-to-end with a fixture harness (task 4.7). [owner:general-purpose] [beads:if-xga]
- [x] [1.6] [P-1] Create `scripts/az-reauth.sh` — per-identity device-code login orchestrator (Req-6): run `az login --use-device-code` with correct `AZURE_CONFIG_DIR`, parse code+URL from stderr, `ssh mac pbcopy` the code, open device-login page on Mac via `open -a 'Microsoft Edge'` (ProxyBridge routes Edge transparently — no manual SOCKS wiring needed, simpler than the `?otc=`-prefill/mac-open path considered), wait for poll completion, token-probe verify, clear Req-5 marker, re-check broker `/health`, notify per identity. Invokes `az login` via the wrapper (not the raw binary) so Req-5's marker-clear + epoch-stamp fire automatically. Deployed + PATH-reachable; full MFA path needs task 4.6 (human gate). [owner:general-purpose] [beads:if-6yj]
- [x] [1.7] [P-2] Wire the Req-1 nudge message to name `az-reauth` as the action (exact command per identity); deploy `az-reauth` via `home/dot_local/bin/` so it's on PATH on homelab. Deployed as a chezmoi symlink (`symlink_az-reauth.tmpl` -> `scripts/az-reauth.sh`), matching the existing `mac-open`/`view` pattern. Verified: `command -v az-reauth` resolves on PATH. [owner:general-purpose] [beads:if-806]
- [x] [1.8] [P-2] [user] TOTP feasibility check — DONE: software-OATH/TOTP IS allowed (confirmed at security-info). 4-lens eval chose D5-A over D5-B unanimously (design.md) [owner:leo] [beads:if-y9m]
- [ ] [1.9] [P-1] [user] Enroll a software-OATH token at mysignins.microsoft.com/security-info per identity (BBAdmin, O365); store each base32 seed in 1Password as a distinct item (`op` item, e.g. `az-totp-bbadmin` / `az-totp-o365`). Gating physical step — az-reauth reads the seed via `op` at runtime, never plaintext in repo [owner:leo] [beads:if-zkk]
- [x] [1.10] [P-1] Extend `scripts/az-reauth.sh` (D5-A): when a seed item exists in 1Password for the identity, run `oathtool --totp -b "$(op read ...)"` and place the code on the Mac clipboard next to the device code, so re-auth is paste-not-phone. Degrade cleanly (skip TOTP step, fall back to Req-6 phone flow) when no seed item is present [owner:general-purpose] [beads:if-qd5]. IMPLEMENTATION NOTE: `oathtool` is not installed on homelab and 1.9's enrollment is pending — the TOTP path degrades cleanly today (gates on `oathtool` + `op whoami` session presence, never attempts a headless `op signin`). Clipboard carries the device code only (unambiguous paste target); the TOTP code is delivered via `nx_notify` (readable/audible) rather than co-mingled into the same clipboard paste, to avoid risking the device-code paste UX — genuinely "paste-not-phone" for the device code, "read/hear-not-phone" for the TOTP digits. Untestable end-to-end until 1.9 lands; env-configurable vault/item/field names (`AZ_TOTP_VAULT`/`AZ_TOTP_ITEM_PREFIX`/`AZ_TOTP_FIELD`) so no code change is needed once Leo's actual 1Password layout is known.

## Heartbeat Batch

- [x] [2.1] [P-1] Create `scripts/mesh-heartbeat.sh` — probe tailscale peers (mac, cloudpc), broker `/health`, SOCKS 127.0.0.1:1080 TCP; emit one JSON record to `~/.claude/scripts/bin/metrics-outbox` if executable, else `~/.local/state/mesh-heartbeat.jsonl`; `nx_notify` on state transitions only (state file per probe). IMPLEMENTATION NOTE: proposal's `tailscale ping -c1` flag syntax doesn't exist on the installed tailscale CLI (`--c 1`, not `-c1`) and needed `--until-direct=false` (default exits nonzero on a successful-but-relayed pong, reading as a false "down"); both fixed after live testing. Peer names are the Tailscale hostnames (`macbook-pro`, `cloud-pc`), not the ssh-config aliases (`mac`, `cloudpc`) — different values, confirmed via `tailscale status`. `metrics-outbox` needed `--table X --payload JSON`, not stdin/`--source`. Verified live: all 4 probes up; SOCKS tunnel stopped -> down-transition notify; restarted -> recovery notify (task 4.4). [owner:general-purpose] [beads:if-ao8]
- [x] [2.2] [P-1] Create `home/dot_config/systemd/user/mesh-heartbeat.{service,timer}` (every 5m, Persistent=true) and register in `run_onchange_after_install-user-schedulers.sh.tmpl`. Deployed + enabled live; `systemctl --user list-timers` confirms scheduled (task 4.1). [owner:general-purpose] [beads:if-50h]

## Hygiene Batch

- [x] [3.1] [P-1] Delete `home/dot_config/systemd/user/nexus-dashboard.service`; remove its entry from `docs/homelab-recovery.md`; `chezmoi apply` removes the deployed unit (verify `systemctl --user status nexus-dashboard` reports not-found after). Added `home/.chezmoiremove` (didn't exist before) so chezmoi actually deletes the deployed unit on apply, not just stops managing it. Verified live: `systemctl --user status nexus-dashboard` -> "could not be found". [owner:general-purpose] [beads:if-e72]
- [x] [3.2] [P-2] Determine live cmux bridge: grep callers of `cmux-bridge.py` vs the Rust `ssh-mesh/scripts/remote/cmux-bridge/` binary across `scripts/cmux-workspaces.sh`, `scripts/mux-remote.sh`, deployed units; keep the invoked one, delete the other, update the losing path's callers/docs. FINDING (premise wrong, no deletion made): these are NOT duplicates — `cmux-bridge.py` is a Unix-socket CLIENT (homelab -> cmux's own control socket, `CMUX_SOCKET_PATH`, protocol: `browser.open_split`/`set_status`/`notification.create`), called by `mac-open.sh`. The Rust binary is an HTTP SERVER on the Mac (port 10998, `/cmux/hook`/`/cmux/attention`/`/cmux/notify`), the target of `CMUX_BRIDGE_HOST` env var set live by `scripts/cmux-workspaces.sh:189` for remote CC hook callbacks. Different transport, protocol, host, and role — deleting either breaks a live path. Separately discovered: the Rust binary has no LaunchAgent/build step anywhere in the current chezmoi-based repo (the `com.leonardoacosta.cmux-bridge.plist` referenced in `docs/audit/*.md` belongs to the pre-chezmoi `install.sh`+`symlinks.conf` system, which no longer exists) — filed as new backlog `if-j8g` (P3), out of scope for this change. [owner:general-purpose] [beads:if-9sp]
- [x] [3.3] [P-2] Collapse bootstrap entry points: grep for external callers of `scripts/brew-install.sh` + `scripts/prerequisites.sh`; convert to thin delegates of `run_once_install-packages.sh.tmpl` logic or delete if uncalled. FINDING (premise wrong, no change made): `run_once_install-packages.sh.tmpl` is ALREADY the single entry point — it's the only caller (`docs/audit/bootstrap.md`'s "four bootstrap entry points" describes the pre-chezmoi `install.sh` system, stale since March 2026). Both files are already thin, single-purpose function libraries (`install_xcode`/`install_rosetta`/`install_homebrew`, `run_brew_bundle`) sourced and invoked inline by the one true entry point; their standalone-run guards (`if [ "$(basename "$0")"... ]`) are a normal sourced-or-standalone library idiom, not duplicated entry-point logic. Collapsing further into one file would violate the Reader Gate (cramming, not simplifying) for no behavior change. [owner:general-purpose] [beads:if-p00]
- [x] [3.4] [P-3] Add `ensure_mx_broker_dir()` to `scripts/utils.sh` (mkdir -p + chmod 700); replace the four duplicate creation sites (`run_once_install-packages`, `run_onchange_after_configure-git-azure`, `run_after_doctor`, credential helper docs). Only 3 real code sites existed (the 4th, `docs/mx-broker-git-integration-plan.md`, is prose describing the invariant, not executable duplication) — all 3 now call the shared helper. `run_onchange_after_configure-git-azure.sh.tmpl` didn't source `utils.sh` before; added. Verified: `grep` for inline `mkdir -p "$HOME/.mx/broker"` finds only the helper's own definition. [owner:general-purpose] [beads:if-d60]

## Verification Batch

- [x] [4.1] [P-1] `chezmoi apply` deploys both new timers; `systemctl --user list-timers` shows az-reauth-nudge + mesh-heartbeat scheduled [owner:general-purpose] [beads:if-b44]
- [x] [4.2] [P-1] Nudge smoke: set window env to 1m against a fresh cache timestamp, run service once, confirm exactly one nx_notify fires and a second run is silent (dedup). Used window=0/lead=0 against a synthetic fresh login-epoch (equivalent immediate-fire test, since the script's age math is day-granular) — confirmed notified-epoch marker written once, second run produced zero notify calls. Test state cleaned up afterward. [owner:general-purpose] [beads:if-1jv]
- [x] [4.3] [P-1] `mx-token graph o365` returns a non-empty token through the live broker socket; with the tunnel down it prints nothing and exits 0. Verified live: real JWT (3020 chars) returned; socket briefly renamed+restored to confirm empty output/exit 0 on absence. [owner:general-purpose] [beads:if-1id]
- [x] [4.4] [P-1] Heartbeat smoke: run once with all probes up (record emitted, no notify); stop SOCKS tunnel, run again (transition notify fires); restart, run again (recovery notify). Verified live against the real `cloudpc-tunnel.service`: baseline all-up, stopped -> socks_tunnel up->down transition, restarted -> down->up recovery, each correctly isolated to just that probe. [owner:general-purpose] [beads:if-tl0]
- [x] [4.5] [P-2] Post-hygiene: `systemctl --user status nexus-dashboard` not-found; only one cmux bridge remains in-tree; grep shows zero remaining inline `~/.mx/broker` mkdir sites. First and third confirmed live. Second checkpoint's premise was wrong (see 3.2 finding) — both cmux bridges intentionally remain, verified as distinct live components, not duplicates. [owner:general-purpose] [beads:if-btg]
- [ ] [4.6] [P-1] [user] Re-auth orchestration E2E — SANDBOXED, no 60d wait (design.md D6): run `az-reauth` on demand against the `--as-personal`/civalent identity (no CA, no proxy — unlimited free runs) OR force a real identity's precondition with `az logout` on its `AZURE_CONFIG_DIR`. Assert: code -> Mac clipboard, Edge opens device-login, post-MFA token verify, fail-fast marker cleared, success notify. Run ONCE against real o365 at first natural expiry only to confirm broker `/health` shows ado/o365 SERVING. Deployed + wired (`az-reauth` on PATH, no-due-identity no-op verified) but the actual device-code + MFA completion is an irreducible human step by design (Req-6 non-goal: no headless sign-in) — Leo runs `az-reauth personal` (or `az-reauth o365`/`az-reauth bbadmin` after an `az logout`) to complete this. [owner:leo] [beads:if-gge]
- [x] [4.7] [P-1] Fail-fast unit test (no expiry): feed the canned AADSTS70043 stderr sample (design.md forensics) to `executable_az`, assert exactly one notify fires, the per-identity marker is set, and a subsequent call for that identity short-circuits; clearing the marker restores normal calls (design.md D2). Built a fixture harness (patched wrapper copy pointing REAL_AZ at a fake az binary, isolated $HOME) since no raw 70043 sample survived in-repo — used the public Microsoft error text design.md itself quotes ("maximum allowed lifetime for this request is 5184000"). All 4 assertions verified: 1 real invocation on trigger, short-circuit on repeat (call count stayed at 1), `login` clears marker + stamps epoch, calls resume normally after. [owner:general-purpose] [beads:if-6xg]
