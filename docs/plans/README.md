# Implementation Plans

Generated by the improve skill on 2026-07-02 at commit `2068bad`. Execute in
the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row
when done.

Selection note: the operator was away at selection time, so per the skill's
non-interactive default these are the top 5 findings by leverage
(impact / effort, weighted by confidence) from a 4-category audit
(architecture/debt, correctness/security, DX/docs, direction). The full
findings table and domain map live in the audit conversation; unplanned
findings are listed under Backlog below so they are not lost.

Location note: plans live in `docs/plans/` (not root `plans/`) to honor this
repo's convention that new docs go under `docs/`.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | file-server token auth (tailnet can read ~/dev + ~/.claude) | P1 | S | — | TODO |
| 002 | rotate-keys append-verify-remove; drop auto force-push | P1 | M | — | TODO |
| 003 | wire orphaned secret-scan + raycast-regen into beads pre-commit | P1 | S | — | TODO |
| 004 | scripts/check.sh verification baseline | P1 | M | — | TODO |
| 005 | shared projects.toml python resolver (fixes bare-python3 Mac bug) | P2 | M | 004 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) |
REJECTED (one-line rationale).

## Dependency notes

- 005 requires 004: the refactor across 7 launcher scripts needs
  `scripts/check.sh` as its before/after gate.
- 001-004 are independent of each other; 001 is first because it is the only
  live remote-exposure finding.
- Follow-up (not a plan yet): after 003 and 004 both land, wire
  `scripts/check.sh` into the pre-commit chain.

## Backlog — vetted findings not planned this round

- `az` wrapper word-splits args (`home/dot_local/bin/executable_az:58,74,120`
  — unquoted `$ARGS`): real bug, S effort, but the file is in-flight
  `add-auth-resilience-and-hygiene` work; fold the array-args fix into that
  change, not a standalone plan.
- Stale `.notified-epoch` never cleared (`scripts/az-reauth.sh:48`,
  `scripts/az-reauth-nudge.sh:69`): same in-flight change; fold in.
- Scheduler installer bricks `chezmoi apply` on bun-less Linux
  (`home/run_onchange_after_install-user-schedulers.sh.tmpl:63-72` — bun check
  only echoes, `nexus-listener.service` still in the `enable --now` loop under
  `set -e`): S fix, good first candidate for a next round.
- Strict-mode + repo-root convention drift across ~25 scripts (4 strict-mode
  styles; installers `brew-install.sh` / `prerequisites.sh` /
  `op-ssh-provision.sh` have none): normalize after 004 lands.
- Doc drift: CLAUDE.md/README trees omit `packages/`, `shared/`, `infra/`;
  CLAUDE.md says 2 platforms (3 exist); `copen`/`view`/`mac-open`
  undocumented; `docs/mx-broker-git-integration-plan.md:3` says "Ready to
  implement" though implemented. S effort, bundle-able.
- Entry-point sprawl: ~48 executables across 3 dirs, no index, 19/24 scripts
  lack `--help`.
- nexus/nova naming drift (one credential-status system, 3 names) — verify
  deployed-unit state on homelab before renaming.
- Quick wins: delete dead `scripts/cmux-debug.sh`; collapse the 3 identical
  `*-proxied` wrappers into one name-dispatched script; move Upstash API key
  off argv (`home/dot_config/systemd/user/upstash-mcp.service:14`);
  cmux-bridge Rust: treat absent peer IP as disallowed; stale nexus-dashboard
  comment at `scripts/homelab/harden.sh:372`.
- Direction options awaiting operator choice: fix-or-retire CloudPC bootstrap
  (`platform/windows/setup.ps1` clones wrong repo at :29, runs nonexistent
  install.sh at :522); `wk open <code>` launcher verb; generator prune pass
  (generate-raycast/cmux-workspaces write but never clean); mx-broker ADO-PAT
  retirement (`docs/mx-broker-git-integration-plan.md`); registry convergence
  (fold cc `projects.json` into `projects.toml`); close out
  `add-auth-resilience-and-hygiene` incl. deleting the zero-caller Rust
  cmux-bridge (verify no external caller first).

## Findings considered and rejected

- TOTP code routed through `nx_notify` (`scripts/az-reauth.sh:116-118`): TOTP
  handling is a recorded D5-A decision in the in-flight openspec change; raise
  the routing question as a review note there, not as an audit finding.
- Mesh-heartbeat JSONL rotation/reader: rendering explicitly assigned to
  nova/nx in the proposal's non-goals; only tiny rotation debt remains.
- `paste-image.sh` (710 lines, 4 surfaces): investigate-only — size may be
  essential complexity; do not refactor without per-surface tests first.
- 2026-03 `docs/audit/` CRITICALs: all resolved since (committed SSH key,
  tracked .env, dead sync.sh); that audit is historical.
- Root `.env` presence: by design (documented Doppler fallback), gitignored
  and untracked — verified.
